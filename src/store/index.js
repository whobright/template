import Vue from 'vue'
import Vuex from 'vuex'
Vue.use(Vuex)

const state = {
    
}
const mutations = {
}

const getters = {
	
}

const store = new Vuex.Store({
  state,
  mutations,
  getters,
});


export default store;
